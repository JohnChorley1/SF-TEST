module.exports = function(Client) {
    /*Client.embedsOne(Client, {
        as: 'about', // Default to the relation name - about
        property: 'about' // default to about.
    });

    Client.embedsOne(Client, {
        as: 'contact', // Default to the relation name - contactDetails.
        property: 'contactDetails'
    });*/
};
